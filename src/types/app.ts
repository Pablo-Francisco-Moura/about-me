export type TypeContact = {
  name: string;
  link: string;
  image: string;
  tooltip: string;
};

export type TypeWork = {
  name: string;
  link: string;
  description: string;
  image: string;
  information: string;
};
