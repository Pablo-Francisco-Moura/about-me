export type TypeContact = {
  name: string;
  link: string;
  image: string;
  tooltip: string;
};

export type TypeWork = {
  title: string;
  link: string;
  image?: string;
  description: string;
};
